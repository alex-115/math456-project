---
title: "CSCI456"
output: pdf_document
date: "2026-02-27"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r}
library(tidyverse)
df <- read_csv("Crime_Data_from_2020_to_2024_20260305.csv")
```

```{r}
# Change the date format into POSIXct datetime
df$`DATE OCC` <- as.POSIXct(df$`DATE OCC`, format = "%Y %b %d %I:%M:%S %p")
```


```{r}
# Convert TIME OCC to integer
df$`TIME OCC` <- as.integer(df$`TIME OCC`)

# Convert AREA to integer
df$AREA <- as.integer(df$AREA)

# Convert Rpt Dist No to integer
df$`Rpt Dist No` <- as.integer(df$`Rpt Dist No`)

# Convert Premis Cd to integer
df$`Premis Cd` <- as.integer(df$`Premis Cd`)
```


```{r}
readr::write_csv(df, "Fixed_Date_version.csv") 
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
